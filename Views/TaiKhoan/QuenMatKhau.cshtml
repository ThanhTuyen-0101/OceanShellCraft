@model OceanShellCraft.Models.ViewModels.QuenMatKhauVM
@{
    ViewData["Title"] = "Quên mật khẩu";
    ViewData["HideHeaderFooter"] = true;
}

<div class="login-scope container-fluid g-0">
    <div class="row g-0 min-vh-100">
        <div class="col-lg-6 d-flex flex-column justify-content-center p-5 position-relative">
            <div class="my-auto animate__animated animate__fadeInLeft">
                <h1 class="display-3 mb-0" style="line-height: 1.1;">Lấy lại <br />truy cập</h1>
            </div>
        </div>

        <div class="col-lg-6 glass-effect d-flex align-items-center justify-content-center p-4 p-md-5">
            <div class="w-100" style="max-width: 420px;">
                <h2 class="mb-5 fs-1">Quên mật khẩu</h2>

                @if (ViewBag.Loi != null)
                {
                    <div class="alert alert-danger bg-danger bg-opacity-10 border-0 text-white small mb-4">
                        @ViewBag.Loi
                    </div>
                }

                <form action="/quen-mat-khau" method="post">
                    @Html.AntiForgeryToken()
                    <div class="mb-4">
                        <label class="mb-2 ps-2 small opacity-75">Nhập Email của bạn</label>
                        <input asp-for="Email" type="email" class="glass-input pill-style" placeholder="example@gmail.com" required />
                    </div>

                    <button type="submit" class="glass-btn w-100 py-2 mb-4 fs-5">
                        Gửi mã xác nhận
                    </button>

                    <div class="text-center mt-5">
                        <a href="/dang-nhap" class="text-decoration-none small opacity-75">Quay lại Đăng nhập</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>